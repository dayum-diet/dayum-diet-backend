spring:
  config:
    activate:
      on-profile: dev

  jpa:
    generate-ddl: true
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update

#  datasource:
#    driver-class-name: org.postgresql.Driver
#    url: jdbc:postgresql://postgresql_db:5432/${POSTGRES_DB}
#    username: ${POSTGRES_USER}
#    password: ${POSTGRES_PASSWORD}